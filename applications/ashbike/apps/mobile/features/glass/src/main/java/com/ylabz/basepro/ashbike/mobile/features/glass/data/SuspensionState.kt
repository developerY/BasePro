package com.ylabz.basepro.ashbike.mobile.features.glass.data

enum class SuspensionState(val label: String) {
    OPEN("Open"),   // Soft / Plush
    TRAIL("Trail"), // Medium / Mixed
    LOCK("Lock")    // Hard / Efficient
}